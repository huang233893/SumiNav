# SumiNav - Windows 98风格导航页

SumiNav是一个基于Vite构建的Windows 98风格的个性化导航页，支持自定义链接、响应式设计和本地数据存储，提供了丰富的配置选项以满足不同用户的需求。

## 🚀 功能特性

- **Windows 98风格界面**：使用98.css模拟经典Windows 98界面风格
- **自定义链接管理**：添加、编辑、删除和拖拽排序导航链接
- **分类管理**：支持链接分类和分类标签切换
- **响应式设计**：适配不同屏幕尺寸的设备
- **本地数据存储**：所有设置和链接保存在浏览器本地
- **主题支持**：多种内置壁纸选择和自定义壁纸支持
- **可配置性**：通过配置文件灵活调整各种参数
- **任务栏功能**：包含开始菜单和GitHub项目链接
- **SEO优化**：支持搜索引擎优化

## 🛠️ 技术栈

- **Vite**：快速的前端构建工具
- **98.css**：Windows 98风格CSS框架
- **Vanilla JavaScript**：原生JavaScript开发
- **HTML5**：语义化HTML结构
- **CSS3**：现代CSS特性，包括Flexbox和Grid布局

## 📦 安装与运行

### 环境要求

- Node.js 16+
- npm 8+ 或 yarn 1.22+

### 安装步骤

1. 克隆仓库

```bash
git clone https://github.com/huang233893/SumiNav.git
cd SumiNav
```

2. 安装依赖

```bash
npm install
```

3. 启动开发服务器

```bash
npm run dev
```

4. 打开浏览器访问 `http://localhost:5173`

## 📁 项目结构

```
SumiNav/
├── dist/                  # 构建输出目录
├── public/                # 静态资源目录
│   └── favicon.ico        # 网站图标
├── src/                   # 源代码目录
│   ├── config.js          # 全局配置文件
│   ├── default-links.js   # 默认链接数据
│   ├── main.js            # 主要JavaScript逻辑
│   └── style.css          # 样式文件
├── index.html             # 主HTML文件
├── package.json           # 项目配置
├── .gitignore             # Git忽略文件
└── README.md              # 项目说明文档
```

## 🎨 使用说明

### 添加新链接

1. 点击「开始」按钮
2. 选择「添加链接」
3. 填写链接名称、URL、图标和分类
4. 点击「保存」按钮

### 编辑或删除链接

1. 右键点击桌面图标
2. 选择「编辑」或「删除」

### 重新排列图标

- 长按桌面图标（或点击拖动）进行排序

### 分类管理

- 点击分类标签可以过滤显示不同分类的链接
- 新添加的分类会自动添加到分类列表中

### 更改设置

1. 点击「开始」按钮
2. 选择「设置」
3. 选择壁纸和任务栏位置
4. 点击「保存」按钮

### 访问GitHub项目

- 点击任务栏中的「此主题项目」按钮即可访问GitHub仓库

## ⚙️ 配置选项

SumiNav提供了丰富的配置选项，所有配置都集中在 `src/config.js` 文件中：

### 基本配置
```javascript
const appConfig = {
  appName: 'SumiNav',      // 应用名称
  version: '1.0.0',        // 版本号
  startupTime: new Date(Date.now() - 3600000).toISOString().slice(0, 16),  // 应用启动时间
  githubProjectUrl: 'https://github.com/huang233893/SumiNav'  // GitHub项目地址
}
```

### 时间配置
```javascript
time: {
  updateInterval: 60000    // 时间更新间隔（毫秒）
}
```

### 视图配置
```javascript
view: {
  defaultMode: 'grid'      // 默认视图模式：'grid'（网格）或 'list'（列表）
}
```

### 桌面配置
```javascript
desktop: {
  defaultWallpaper: 'default',  // 默认壁纸
  customWallpaper: '',         // 自定义壁纸URL
  taskbarPosition: 'bottom',   // 任务栏位置：'bottom'（底部）或 'top'（顶部）
  longPressDuration: 500,      // 长按触发时间（毫秒）
  dragThreshold: 10            // 拖拽阈值（像素）
}
```

### 链接配置
```javascript
links: {
  defaultIcon: '📂',          // 默认图标
  defaultCategory: '常用',    // 默认分类
  categories: ['工作', '学习', '娱乐', '其他']  // 预设分类列表
}
```

### 菜单配置
```javascript
menu: {
  items: [
    { id: 'add-link', text: '添加链接', icon: '📝' },
    { id: 'toggle-view', text: '切换视图', icon: '🔄' },
    { id: 'settings', text: '设置', icon: '⚙️' },
    { id: 'help', text: '帮助', icon: '❓' },
    { id: 'about', text: '关于', icon: 'ℹ️' },
    { id: 'reset', text: '重置应用', icon: '🗑️' }
  ]
}
```

## 🚀 构建与部署

### 构建生产版本

```bash
npm run build
```

构建后的文件将输出到 `dist` 目录。

### 预览生产版本

```bash
npm run preview
```

### 部署到GitHub Pages

1. 修改 `package.json`，添加以下脚本：

```json
"scripts": {
  "deploy": "npm run build && npx gh-pages -d dist"
}
```

2. 安装 `gh-pages` 依赖：

```bash
npm install --save-dev gh-pages
```

3. 部署到GitHub Pages：

```bash
npm run deploy
```

### 部署到其他平台

你可以将 `dist` 目录下的文件部署到任何静态网站托管服务，如Vercel、Netlify、Cloudflare Pages等。

## 🌐 浏览器支持

- Chrome 88+
- Firefox 85+
- Safari 14+
- Edge 88+

## 🤝 贡献

欢迎提交Issue和Pull Request来改进项目！
